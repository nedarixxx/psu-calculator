# 🚀 QUICKSTART — PSU Calculator

**Полная инструкция по запуску проекта за 10 минут!**

---

## 📋 Содержание

1. [Проверка предварительных условий](#-проверка-предварительных-условий)
2. [Полный пошаговый запуск](#-полный-пошаговый-запуск)
3. [Тестирование проекта](#-тестирование-проекта)
4. [Команды для разработки](#-команды-для-разработки)
5. [Решение ошибок](#-решение-ошибок)
6. [Быстрая справка](#-быстрая-справка)

---

## ✅ Проверка предварительных условий

### 1. Python версия

```bash
# Windows
python --version

# Linux / Mac
python3 --version
```

**Результат должен быть:** `Python 3.10+`

Если версия ниже, скачай Python с https://python.org

### 2. Git установлен?

```bash
git --version
```

**Результат:** `git version 2.x.x`

Если не установлен: https://git-scm.com/downloads

### 3. Репозиторий клонирован?

```bash
git clone https://github.com/YOUR_USERNAME/psu-calculator.git
cd psu-calculator
```

✅ Если есть папка `psu-calculator` — всё готово!

---

## 🚀 Полный пошаговый запуск

### ⏱️ Ожидаемое время: 8-10 минут

### Шаг 1️⃣: Создай виртуальное окружение (2 минуты)

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux / Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

**✅ Признак успеха:** Слева в терминале появилось `(venv)`

```
(venv) C:\psu-calculator>  ← Видишь (venv)? Отлично!
```

---

### Шаг 2️⃣: Установи зависимости (3-4 минуты)

```bash
pip install -r requirements.txt
```

**Что устанавливается:**
- Django 4.2.7
- Pandas 2.1.3
- Matplotlib 3.8.2
- Pillow 10.1.0

⏳ Дождись окончания (строка "Successfully installed...")

---

### Шаг 3️⃣: Примени миграции БД (30 секунд)

```bash
python manage.py migrate
```

**✅ Ожидаемый результат:**
```
Running migrations:
  Applying contenttypes.0001_initial... OK
  Applying auth.0001_initial... OK
  ...
  Applying calculator.0001_initial... OK
```

---

### Шаг 4️⃣: Создай администратора (1 минута)

```bash
python manage.py createsuperuser
```

**Введи данные:**

```
Username: admin
Email address: admin@example.com
Password: admin123
Password (again): admin123
```

⚠️ **Пароль НЕ отображается при вводе — это нормально!**

---

### Шаг 5️⃣: Заполни БД тестовыми данными (1 минута)

```bash
python manage.py populate_data
```

**✅ Ожидаемый результат:**
```
Creating categories...
Creating components...
Loading data from JSON...
Successfully loaded 100+ components into database
Database population completed!
```

---

### Шаг 6️⃣: Запусти сервер (30 секунд)

```bash
python manage.py runserver
```

**✅ Ожидаемый результат:**
```
Django version 4.2.7
Starting development server at http://127.0.0.1:8000/
Quit the server with CONTROL-C.
```

---

### Шаг 7️⃣: Открой браузер 🌐

Переходи по ссылке (скопируй и вставь в адресную строку):

```
http://127.0.0.1:8000/
```

🎉 **Видишь главную страницу? ВСЁ РАБОТАЕТ!**

---

## 🧪 Тестирование проекта

### Тест 1: Главная страница

**URL:** http://127.0.0.1:8000/

**Ожидается:**
- ✅ Логотип и название проекта
- ✅ Статистика (количество сборок, компонентов, пользователей)
- ✅ Кнопка "Просмотреть компоненты"
- ✅ Кнопка "Войти" или "Выход" (если вошел)

---

### Тест 2: Каталог компонентов (для гостей)

**URL:** http://127.0.0.1:8000/components/

**Ожидается:**
- ✅ Список всех компонентов (100+)
- ✅ Компоненты отсортированы по категориям
- ✅ Для каждого компонента: название, потребление (Вт), цена
- ✅ Поле поиска работает
- ✅ Фильтр по категориям работает

**Тест поиска:**
1. Введи "RTX" в поле поиска
2. Должны остаться только видеокарты с RTX
3. Очисти поле → вернулись все компоненты ✅

---

### Тест 3: Вход в систему

**URL:** http://127.0.0.1:8000/accounts/login/

**Данные для входа:**
```
Email/Username: admin
Пароль: admin123
```

**Ожидается:**
- ✅ Форма входа открывается
- ✅ Система разрешает вход с admin/admin123
- ✅ Перенаправление на страницу сборок
- ✅ В топе страницы видно: "Welcome, admin!" или "Привет, admin!"

---

### Тест 4: Список сборок (только для авторизованных)

**URL:** http://127.0.0.1:8000/builds/

**Ожидается после входа:**
- ✅ Таблица "Мои сборки" (пока пуста или с тестовыми сборками)
- ✅ Кнопка "Создать новую сборку"
- ✅ Кнопка "Выход" в топе

---

### Тест 5: Создание новой сборки

**URL:** http://127.0.0.1:8000/builds/create/

**Действия:**
1. Нажми "Создать новую сборку"
2. Заполни:
   - **Название:** "Мой первый ПК"
   - **Описание:** "Тестовая сборка"
3. Нажми "Создать"

**Ожидается:**
- ✅ Сборка создана
- ✅ Перенаправление на страницу деталей сборки
- ✅ Видна форма "Добавить компонент"

---

### Тест 6: Добавление компонентов в сборку

**На странице деталей сборки:**

1. **Добавь процессор:**
   - В выпадающем списке выбери компонент (например, "Intel Core i7-13700K")
   - Нажми "Добавить"

2. **Добавь видеокарту:**
   - Выбери из списка (например, "NVIDIA RTX 4060 Ti")
   - Нажми "Добавить"

3. **Добавь память:**
   - Выбери RAM (например, "DDR5 32GB")
   - Нажми "Добавить"

**Ожидается:**
- ✅ Компоненты появляются в таблице
- ✅ Показано потребление каждого (в Ватт)
- ✅ Таблица обновляется автоматически

---

### Тест 7: Расчет энергопотребления (ГЛАВНЫЙ ТЕСТ!)

**На странице деталей сборки (после добавления компонентов):**

**Ожидается видеть:**

1. **Таблица компонентов:**
   ```
   Компонент                      | Потребление
   Intel Core i7-13700K           | 125W
   NVIDIA RTX 4060 Ti             | 165W
   DDR5 32GB                      | 15W
   └─ ИТОГО:                      | 305W
   ```

2. **Расчет БП (КРИТИЧНО!):**
   ```
   Суммарное потребление: 305W
   Коэффициент надежности: 1.25
   Рекомендуемая мощность БП: 381W (305 × 1.25)
   ➜ Рекомендуется БП не менее: 450W
   ```

3. **Pie Chart (круговая диаграмма):**
   - ✅ Диаграмма видна (отображает % по категориям)
   - ✅ Показаны цвета разных категорий
   - ✅ Есть легенда с процентами

**Если расчет верный → ВСЁ РАБОТАЕТ! ✅**

---

### Тест 8: Админ-панель

**URL:** http://127.0.0.1:8000/admin/

**Данные:**
```
Username: admin
Пароль: admin123
```

**Ожидается:**
- ✅ Админ-панель открывается
- ✅ Видны разделы: Categories, Components, Builds, Users
- ✅ Можно создавать, редактировать, удалять компоненты
- ✅ Поиск работает (попробуй поискать "RTX")

---

## 📟 Команды для разработки

### Основные команды

```bash
# Запуск сервера
python manage.py runserver

# На другом порту
python manage.py runserver 0.0.0.0:8080

# Создание миграций после изменения моделей
python manage.py makemigrations

# Применение миграций
python manage.py migrate

# Интерактивная консоль Django (python shell)
python manage.py shell

# Заполнение БД тестовыми данными
python manage.py populate_data

# Создание нового администратора
python manage.py createsuperuser

# Очистка БД (опасно!)
python manage.py flush
```

### Команды для статических файлов (production)

```bash
# Собрать все статические файлы в папку /static/
python manage.py collectstatic

# Показать где находятся статические файлы
python manage.py findstatic

# Проверить статические файлы
python manage.py findstatic --list
```

### Команды для тестирования

```bash
# Запустить все тесты
python manage.py test

# Запустить тесты приложения calculator
python manage.py test calculator

# Запустить конкретный тест
python manage.py test calculator.tests.TestBuildModel
```

---

## 🐛 Решение ошибок

### ❌ Ошибка: `No module named 'django'`

**Причина:** Зависимости не установлены

**Решение:**
```bash
# Убедись, что (venv) активирован
pip install -r requirements.txt
```

---

### ❌ Ошибка: `django.db.utils.ProgrammingError`

**Причина:** БД не создана или миграции не применены

**Решение:**
```bash
python manage.py migrate
python manage.py populate_data
```

---

### ❌ Ошибка: `ModuleNotFoundError: No module named 'pandas'`

**Причина:** Pandas не установлен

**Решение:**
```bash
pip install pandas matplotlib pillow
```

---

### ❌ Ошибка: `TemplateDoesNotExist: calculator/base.html`

**Причина:** Шаблоны в неправильной папке

**Решение:** Проверь структуру:
```
calculator/templates/calculator/
                     ↑           ↑
                   app/      app-name/
```

Все шаблоны должны быть в `calculator/templates/calculator/`

---

### ❌ Ошибка: `Pie chart не отображается`

**Причина:** Matplotlib backend не настроен

**Решение:** В `views.py` добавь в начало:
```python
import matplotlib
matplotlib.use('Agg')  # ← Это критично!

import matplotlib.pyplot as plt
```

Затем перезапусти сервер: `python manage.py runserver`

---

### ❌ Ошибка: `Page not found (404)` на `/builds/`

**Причина:** Нужна авторизация

**Решение:**
1. Перейди на http://127.0.0.1:8000/accounts/login/
2. Введи: admin / admin123
3. Попробуй еще раз http://127.0.0.1:8000/builds/

---

### ❌ Ошибка: `(venv) не появляется в терминале`

**Причина:** Виртуальное окружение не активировано

**Решение:**

**Windows:**
```bash
venv\Scripts\activate
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

Проверь, что слева в терминале есть `(venv)`

---

### ❌ Ошибка: `ALLOWED_HOSTS doesn't match`

**Причина:** Не указан локальный хост

**Решение:** В `settings.py`:
```python
ALLOWED_HOSTS = ['127.0.0.1', 'localhost', '0.0.0.0']
```

---

### ❌ Ошибка: `Static files not found (CSS/JS не загружаются)`

**Причина:** Статические файлы не собраны

**Решение (только для production):**
```bash
python manage.py collectstatic --noinput
```

Для разработки это не нужно!

---

## 🔄 Быстрая справка

### Горячие клавиши

```
Ctrl + C        → Остановить сервер
↑ ↓             → История команд в терминале
Ctrl + L        → Очистить экран терминала
```

### Структура проекта (основные файлы)

```
psu-calculator/
├── psu_calculator/          ← Настройки проекта
│   ├── settings.py          ← DEBUG, DATABASES, ALLOWED_HOSTS
│   └── urls.py              ← Главные URL маршруты
│
├── calculator/              ← Основное приложение
│   ├── models.py            ← Category, Component, Build
│   ├── views.py             ← Логика (Pandas, Matplotlib)
│   ├── forms.py             ← Формы Django
│   ├── admin.py             ← Админ-панель
│   ├── urls.py              ← URL маршруты приложения
│   └── templates/           ← HTML шаблоны
│
├── TZ.md                    ← Техническое задание
├── README.md                ← Описание проекта
├── requirements.txt         ← Зависимости
└── db.sqlite3               ← База данных (локально)
```

---

### URL маршруты

| URL | Что это | Требует вход? |
|-----|---------|---------------|
| `/` | Главная страница | ❌ |
| `/components/` | Каталог компонентов | ❌ |
| `/builds/` | Мои сборки | ✅ |
| `/builds/create/` | Создать сборку | ✅ |
| `/builds/<id>/` | Детали сборки | ✅ |
| `/admin/` | Админ-панель | ✅ |
| `/accounts/login/` | Вход | ❌ |

---

### Учетные данные

```
Администратор:
  Логин: admin
  Пароль: admin123

Доступ:
  http://127.0.0.1:8000/admin/
  http://127.0.0.1:8000/accounts/login/
```

---

## 📊 Что использует проект

### Backend
- **Django 4.2.7** — веб-фреймворк
- **Pandas 2.1.3** — обработка данных
- **Matplotlib 3.8.2** — графики
- **Pillow 10.1.0** — обработка изображений

### Frontend
- **Bootstrap 5.3** — дизайн и компоненты
- **HTML5 / CSS3** — разметка и стили
- **JavaScript** — интерактивность

### База данных
- **SQLite** (разработка)
- **PostgreSQL** (production на PythonAnywhere)

---

## ✅ Контрольный список

После запуска проверь:

- [ ] Сервер запустился без ошибок
- [ ] Главная страница открывается (http://127.0.0.1:8000/)
- [ ] Админ-панель доступна (http://127.0.0.1:8000/admin/)
- [ ] Вход работает (admin / admin123)
- [ ] Каталог компонентов отображает 100+ компонентов
- [ ] Можно создать сборку
- [ ] Можно добавить компоненты в сборку
- [ ] Pie chart отображается
- [ ] Расчет БП верный (сумма × 1.25)
- [ ] Поиск и фильтры работают

**Если всё галочкой ✅ — проект полностью готов!**

---

## 🎯 Следующие шаги

### Если хочешь разрабатывать:

1. **Добавить новый компонент:**
   ```
   Перейти в админ-панель → Components → Add Component
   ```

2. **Изменить модель:**
   ```bash
   # Отредактируй calculator/models.py
   python manage.py makemigrations
   python manage.py migrate
   ```

3. **Добавить функциональность:**
   - Редактируй `calculator/views.py`
   - Обновляй шаблоны в `calculator/templates/calculator/`
   - Перезапусти сервер: `python manage.py runserver`

### Если готов к деплою:

1. Прочитай раздел "Деплой на PythonAnywhere" в README.md
2. Подготовь `settings.py` для production
3. Сделай коммит: `git commit -m "Ready for deploy"`
4. Отправь на GitHub: `git push origin main`
5. Следуй инструкциям на PythonAnywhere

---

## 📚 Полезные ресурсы

- **Django документация:** https://docs.djangoproject.com/
- **Pandas документация:** https://pandas.pydata.org/docs/
- **Matplotlib документация:** https://matplotlib.org/
- **Bootstrap документация:** https://getbootstrap.com/docs/5.3/
- **Git справка:** https://git-scm.com/doc

---

## 💬 Возникли проблемы?

**Проверь:**

1. Версия Python: `python --version` (должно быть 3.10+)
2. Активирован ли venv: слева в терминале должно быть `(venv)`
3. Установлены ли зависимости: `pip list | grep django`
4. Миграции применены: `python manage.py migrate`
5. Сервер запущен: `python manage.py runserver`
6. Правильный URL: `http://127.0.0.1:8000/` (не 127.0.0.8000)

---

**🎉 Готово! Проект запущен и готов к разработке!**

**Удачи! 🚀**
